/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ["images.pexels.com", "upload.wikimedia.org","encrypted-tbn0.gstatic.com"],
  },
};

module.exports = nextConfig;
